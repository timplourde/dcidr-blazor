Inline SVG must be used for currentColor to work.

We are inlining only those we need to right now.

These are copies odf the SVG icons in the images directory which come from https://feathericons.com/
